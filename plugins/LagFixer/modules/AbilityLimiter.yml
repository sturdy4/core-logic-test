AbilityLimiter:
  enabled: true
  worlds: # Use '*' to enable for all worlds.
    - '*'
  values:
    elytra_boost:
      cooldown: 5
      # Option disabled by developer
      speed_multiplier: 0.85
      # Set 0 to disable
      additional_durability_loss: 5
    
    trident_riptide:
      cooldown: 3
      # Option disabled by developer
      speed_multiplier: 0.85
      # Set 0 to disable
      additional_durability_loss: 5
